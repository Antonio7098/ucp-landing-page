import { HTMLAttributes, ReactNode } from 'react';
export type PricingCardVariant = 'default' | 'featured' | 'bordered';
export interface PricingFeature {
    text: string;
    included: boolean;
    tooltip?: string;
}
export interface PricingCardProps extends HTMLAttributes<HTMLDivElement> {
    name: string;
    description?: string;
    price: string | ReactNode;
    priceDescription?: string;
    features: PricingFeature[];
    action?: ReactNode;
    variant?: PricingCardVariant;
    badge?: string;
    popular?: boolean;
}
export declare function PricingCard({ name, description, price, priceDescription, features, action, variant, badge, popular, className, ...props }: PricingCardProps): import("react/jsx-runtime").JSX.Element;
