import { HTMLAttributes, ReactNode } from 'react';
export type CodeWindowVariant = 'default' | 'minimal' | 'gradient' | 'glass';
export interface CodeWindowProps extends HTMLAttributes<HTMLDivElement> {
    code: string;
    language?: string;
    filename?: string;
    showLineNumbers?: boolean;
    highlightLines?: number[];
    variant?: CodeWindowVariant;
    showCopy?: boolean;
    header?: ReactNode;
}
export declare function CodeWindow({ code, language, filename, showLineNumbers, highlightLines, variant, showCopy, header, className, ...props }: CodeWindowProps): import("react/jsx-runtime").JSX.Element;
