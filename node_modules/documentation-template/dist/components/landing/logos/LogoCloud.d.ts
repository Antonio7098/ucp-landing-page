import { HTMLAttributes, ReactNode } from 'react';
import { ContainerSize } from '../container/Container';
export type LogoCloudVariant = 'default' | 'muted' | 'bordered' | 'cards';
export type LogoCloudLayout = 'inline' | 'grid' | 'scroll';
export interface LogoItem {
    name: string;
    logo: ReactNode;
    href?: string;
}
export interface LogoCloudProps extends HTMLAttributes<HTMLElement> {
    title?: string;
    logos: LogoItem[];
    variant?: LogoCloudVariant;
    layout?: LogoCloudLayout;
    containerSize?: ContainerSize;
    columns?: 4 | 5 | 6 | 8;
}
export declare function LogoCloud({ title, logos, variant, layout, containerSize, columns, className, ...props }: LogoCloudProps): import("react/jsx-runtime").JSX.Element;
