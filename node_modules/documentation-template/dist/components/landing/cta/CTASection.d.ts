import { HTMLAttributes, ReactNode } from 'react';
import { ContainerSize } from '../container/Container';
import { GradientPreset } from '../typography/GradientText';
export type CTAVariant = 'default' | 'centered' | 'split' | 'card';
export interface CTAAction {
    label: string;
    href?: string;
    onClick?: () => void;
    variant?: 'primary' | 'secondary' | 'outline' | 'ghost';
    icon?: ReactNode;
}
export interface CTASectionProps extends HTMLAttributes<HTMLElement> {
    title: ReactNode;
    description?: string;
    actions?: CTAAction[];
    variant?: CTAVariant;
    gradient?: GradientPreset;
    containerSize?: ContainerSize;
    backgroundElement?: ReactNode;
    media?: ReactNode;
}
export declare function CTASection({ title, description, actions, variant, gradient, containerSize, backgroundElement, media, className, ...props }: CTASectionProps): import("react/jsx-runtime").JSX.Element;
