import { HTMLAttributes, ReactNode } from 'react';
export type HeroBadgeVariant = 'default' | 'outline' | 'gradient' | 'glow';
export interface HeroBadgeProps extends HTMLAttributes<HTMLDivElement> {
    children: ReactNode;
    variant?: HeroBadgeVariant;
    icon?: ReactNode;
    href?: string;
    showArrow?: boolean;
}
export declare function HeroBadge({ children, variant, icon, href, showArrow, className, ...props }: HeroBadgeProps): import("react/jsx-runtime").JSX.Element;
