import { HTMLAttributes, ReactNode } from 'react';
import { ContainerSize } from '../container/Container';
import { GradientPreset } from '../typography/GradientText';
export type HeroLayout = 'centered' | 'split' | 'split-reverse';
export type HeroSize = 'sm' | 'md' | 'lg' | 'xl';
export interface HeroAction {
    label: string;
    href?: string;
    onClick?: () => void;
    variant?: 'primary' | 'secondary' | 'outline' | 'ghost';
    icon?: ReactNode;
    iconPosition?: 'left' | 'right';
}
export interface HeroProps extends Omit<HTMLAttributes<HTMLElement>, 'title'> {
    title: ReactNode;
    titleGradient?: ReactNode;
    titleGradientPreset?: GradientPreset;
    subtitle?: ReactNode;
    badge?: ReactNode;
    actions?: HeroAction[];
    media?: ReactNode;
    layout?: HeroLayout;
    size?: HeroSize;
    containerSize?: ContainerSize;
    backgroundElement?: ReactNode;
    titleClassName?: string;
    subtitleClassName?: string;
    contentAlign?: 'left' | 'center' | 'right';
}
export declare function Hero({ title, titleGradient, titleGradientPreset, subtitle, badge, actions, media, layout, size, containerSize, backgroundElement, className, titleClassName, subtitleClassName, contentAlign, ...props }: HeroProps): import("react/jsx-runtime").JSX.Element;
