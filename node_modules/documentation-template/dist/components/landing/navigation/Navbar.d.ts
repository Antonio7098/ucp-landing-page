import { HTMLAttributes, ReactNode } from 'react';
export interface NavLink {
    label: string;
    href?: string;
    onClick?: () => void;
    children?: NavLink[];
}
export interface NavbarProps extends HTMLAttributes<HTMLElement> {
    logo: ReactNode;
    links?: NavLink[];
    actions?: ReactNode;
    variant?: 'default' | 'transparent' | 'bordered';
    sticky?: boolean;
    mobileBreakpoint?: 'sm' | 'md' | 'lg';
}
export declare function Navbar({ logo, links, actions, variant, sticky, mobileBreakpoint, className, ...props }: NavbarProps): import("react/jsx-runtime").JSX.Element;
